# Tusome AI — Correct Render Structure

This package contains the exact `tusome-ai.html` file supplied for the existing Tusome EduShelf Render project.

IMPORTANT: Replace ONLY the existing `tusome-ai.html` in the current Tusome EduShelf repository.
Do NOT replace `index.html` or `server.mjs` with a newly generated structure.

The supplied Tusome AI page already contains the structure we built, including:
- Chat history drawer
- Conversation search
- Conversation rename/delete
- New chat
- Fast / Balanced / Deep thinking levels
- Study Mode
- Multiple file attachments
- Persistent conversation/file handling in the browser
- MathJax rendering
- PDF/Word export controls
- Existing EduShelf return link

Then commit/push the one-file change to GitHub and let Render redeploy the existing service.
