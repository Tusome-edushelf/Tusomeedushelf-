# Tusome EduShelf v69 — School Account Access Fix

The School Plan Dashboard now has a prominent School Account Access panel at the top with:
- School Sign In
- Create School Account

The Home page For Schools button opens the School Dashboard access flow directly.
Existing school registration endpoint, school role, PostgreSQL setup, school dashboard features and M-PESA Daraja configuration are preserved.

Deploy both `index.html` and `server.mjs`, then restart the Node/Render service.
